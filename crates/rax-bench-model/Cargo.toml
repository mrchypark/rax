[package]
name = "rax-bench-model"
version = "0.4.3"
edition = "2021"
publish = false

[dependencies]
serde = { version = "1.0.228", features = ["derive"] }
strum = { version = "0.27.2", features = ["derive"] }

[lib]
path = "src/lib.rs"
