# Backup/Restore Fixtures

- Keep fixtures produced by Swift Wax backup flow.
- Include base + incremental chain metadata and expected restore target.
- Record whether scenario is full restore, incremental restore, or PITR.
